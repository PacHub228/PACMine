# build output
out/
launcher/out/

# downloaded LWJGL jars (run get-deps.sh to fetch)
lib/*.jar

# saved worlds
saves/

# editor/working files
assets/*.kra
*.class

# vendored mesa (lives on the 'mesa' branch, not main)
mesa-win.zip
launcher/*.jar
